// ray-color/view/webgl — same registration as ray-color/view (the WebGL2
// preview always needs the CPU engine underneath for exact palettes), kept
// as its own entry so imports read as intent.
export * from './view';
